---
name: xhigh
description: この応答だけ effort level を xhigh にオーバーライドする（Opus 4.7 のデフォルト相当、max より軽い）。`/xhigh <instruction>` の形で使う。
effort: xhigh
disable-model-invocation: true
---

# Xhigh Effort Override

この応答では xhigh effort level で処理する。
skill が終わると元の session effort level に戻る。

## タスク

$ARGUMENTS
